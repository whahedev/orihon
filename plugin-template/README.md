# Orihon Plugin Template

Rename the package, implement the layer in `src/index.ts`, and keep `Orihon` as a peer dependency. Before publishing, verify add/remove cycles, map destruction, keyboard accessibility for controls and request cancellation for network-backed layers.

```sh
npm install
npm run typecheck
npm run build
```
